<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <style>
      .custom-btn {
        border: none;
        border-radius: 50px;
        padding: 10px 20px;
        transition: background-color 0.3s ease, transform 0.3s ease;
      }

      .custom-btn.login-btn {
        background-color: #007bff;
        color: white;
      }

      .custom-btn.login-btn:hover {
        background-color: #0056b3;
        transform: scale(1.05);
      }

      .custom-btn.signup-btn {
        background-color: #28a745;
        color: white;
      }

      .custom-btn.signup-btn:hover {
        background-color: #218838;
      }

      .custom-btn.forgot-btn {
        background-color: #6c757d;
        color: white;
      }

      .custom-btn.forgot-btn:hover {
        background-color: #5a6268;
      }

      .custom-btn-block {
        display: block;
        width: 100%;
        margin-bottom: 10px;
      }

      .form-control.custom-input {
        border-radius: 50px;
        padding: 10px 20px;
        border: 1px solid #ced4da;
        transition: border-color 0.3s ease;
      }

      .form-control.custom-input:focus {
        border-color: #007bff;
        box-shadow: 0 0 5px rgba(0, 123, 255, 0.5);
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="row justify-content-center">
        <div class="col-md-6">
          <% if (messages.error) { %>
          <div class="alert alert-danger"><%= messages.error %></div>
          <% } %>
          <h2 class="text-center">Login</h2>
          <form action="/login" method="POST">
            <div class="form-group">
              <label for="EmailOrNumber">Enter email or phone number</label>
              <input
                type="text"
                class="form-control custom-input"
                id="EmailOrNumber"
                name="EmailOrNumber"
                required
              />
            </div>
            <div class="form-group">
              <label for="password">Enter Password</label>
              <input
                type="password"
                class="form-control custom-input"
                id="password"
                name="password"
                required
              />
            </div>
            <button type="submit" class="btn custom-btn login-btn btn-block">
              Login
            </button>
          </form>
          <div class="mt-3">
            <a href="/signup" class="btn custom-btn signup-btn custom-btn-block"
              >Sign Up</a
            >
            <a
              href="/forgetpassword"
              class="btn custom-btn forgot-btn custom-btn-block"
              >Forgot Password</a
            >
          </div>
        </div>
      </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/jquery@3.6.0/dist/jquery.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
